{
  "name": "ss-display-webos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build && npm run validate:package",
    "package:webos": "npm run build && ares-package dist -o artifacts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "check": "npm run lint && npm run test && npm run build",
    "validate:package": "node scripts/validate-package.mjs"
  },
  "dependencies": {
    "@microsoft/signalr": "10.0.11",
    "dompurify": "3.4.13"
  },
  "devDependencies": {
    "@eslint/js": "9.39.2",
    "@types/node": "24.10.1",
    "eslint": "9.39.2",
    "globals": "16.4.0",
    "jsdom": "26.1.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.46.3",
    "vite": "8.2.1",
    "vitest": "4.1.0"
  }
}
